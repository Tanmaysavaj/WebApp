<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Projects - Climate Solutions</title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <%- include('partials/navbar', { page: '/solutions/projects' }) %>

    <main class="container mx-auto my-8">
        <section class="bg-gray-800 p-10 rounded-lg text-center">
            <h1 class="text-5xl font-bold mb-4">Projects</h1>
            <p class="text-lg mb-6">Browse the current list of Projects in our database. Click the theme to filter by sectors such as:</p>
            <div class="flex justify-center space-x-4">
                <a href="/solutions/projects?sector=Industry" class="btn btn-green">Industry</a>
                <a href="/solutions/projects?sector=Transportation" class="btn btn-green">Transportation</a>
                <a href="/solutions/projects?sector=Electricity" class="btn btn-green">Electricity</a>
            </div>
        </section>

        <table class="table-auto w-full mt-8">
            <thead>
                <tr>
                    <th>Title</th>
                    <th>Sector</th>
                    <th>Summary</th>
                    <th>Details</th>
                </tr>
            </thead>
            <tbody>
                <% projects.forEach(project => { %>
                <tr>
                    <td><%= project.title %></td>
                    <td><a href="/solutions/projects?sector=<%= project.Sector.sector_name %>" class="btn btn-xs"><%= project.Sector.sector_name %></a></td>                    <td><%= project.summary_short %></td>
                    <td><a href="/solutions/projects/<%= project.id %>" class="btn btn-details">details</a></td>
                </tr>
                <% }); %>
            </tbody>
        </table>
    </main>
</body>
</html>