<nav class="bg-gray-800 p-4">
    <div style="display: flex; justify-content: space-between; align-items: center; max-width: 1200px; margin: 0 auto;">
        <a href="/" class="text-2xl font-bold text-white">Climate Solutions</a>
        <ul style="display: flex; gap: 2rem; list-style: none; padding: 0; margin: 0;">
            <li>
                <a href="/solutions/projects" class="<%= (page === '/solutions/projects') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">View Projects</a>
            </li>
            <li>
                <a href="/about" class="<%= (page === '/about') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">About</a>
            </li>

            <% if (session && session.user) { %>
                <li>
                    <a href="/solutions/addProject" class="<%= (page === '/solutions/addProject') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">Add Project</a>
                </li>
                <li>
                    <a href="/userHistory" class="<%= (page === '/userHistory') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">User History</a>
                </li>
                <li>
                    <a href="/logout" class="text-white hover:text-green-400">Logout</a>
                </li>
            <% } else { %>
                <li>
                    <a href="/login" class="<%= (page === '/login') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">Login</a>
                </li>
                <li>
                    <a href="/register" class="<%= (page === '/register') ? 'font-bold text-green-400' : 'text-white' %> hover:text-green-400">Register</a>
                </li>
            <% } %>
        </ul>
    </div>
</nav>
